🔹 DHT22 (температурен датчик)

VCC → 3.3V на NodeMCU

GND → GND на NodeMCU

DATA → D2 (GPIO4)

🔹 Relay module (за вентилаторите)

VCC → 5V на NodeMCU (ако модулът е 5V)

GND → GND на NodeMCU

IN → D5 (GPIO14)

⚠️ Забележка: ако релето е 3.3V съвместимо – може и на 3.3V, но повечето са 5V.

🔹 OLED дисплей (SSD1306, I²C)

VCC → 3.3V (някои модели работят и на 5V, но препоръчвам 3.3V за ESP8266)

GND → GND на NodeMCU

SDA → D1 (GPIO5)

SCL → D6 (GPIO12)

📌 Обобщена таблица:
Устройство	Пин на NodeMCU	GPIO
DHT22 DATA	D2	GPIO4
Relay IN	D5	GPIO14
OLED SDA	D1	GPIO5
OLED SCL	D6	GPIO12
Всички VCC	3.3V или 5V*	–
Всички GND	GND	–

* DHT22 → 3.3V или 5V (работи и на двете), OLED → 3.3V (сигурно), Relay → 5V (най-често).

Как да вържеш вентилаторите през релето

Вентилаторите имат +5V (червен) и GND (черен).

+5V кабел → директно от адаптера към вентилаторите (без реле).

GND кабел → минава през релето:

GND от адаптера → COM на релето

NO на релето → GND на вентилаторите

-----------------------

Пинове / кабели — таблица (точно спрямо NodeMCU пинове)
Устройство	Захранване	Сигнал / управление
NodeMCU	USB 5V (microUSB) → VIN вътрешно, GND	—
Relay VCC	+5V от адаптера (→ VCC на релето)	Relay IN → D5 (GPIO14)
Relay GND	GND (обща маса)	—
Relay (силови клеми) COM	GND от адаптера (вход)	—
Relay (силови клеми) NO	към GND на вентилатор(и)	—
Вентилатори +	+5V от адаптера → напр. директно към + на вентилаторите	—
Вентилатори GND	свързан към NO на релето	—
DHT22 VCC	3.3V (или 5V по спецификация)	DHT22 DATA → D2 (GPIO4)
DHT22 GND	GND (обща)	—
OLED VCC	3.3V	OLED SDA → D1 (GPIO5), OLED SCL → D6 (GPIO12)
OLED GND	GND (обща)	—
------------------------------------

Стъпка по стъпка свързване (практически)

Подгответе адаптер 5V (2A+) и оставете кабелите готови (плюс и минус).

NodeMCU: включете го към адаптера чрез microUSB. Това осигурява 5V към Vin и GND към платката.

Relay VCC: свържете към +5V на адаптера (може да използвате Vin пин на NodeMCU, но по-добре директно от адаптера/USB кабела).

Relay GND: свържете към GND на адаптера и към GND на NodeMCU. (ВСИЧКИ GND свързани заедно).

Relay IN: свържете към D5 на NodeMCU (пин за управление).

Входящ GND за релето (силова част): свържете GND от адаптера към COM на релето (това е „входящата“ маса за вентилаторите).

NO: свържете NO към GND на вентилаторите. Така при активирано реле COM ще се свърже с NO и вентилаторите ще получат маса.

+ на вентилаторите: свържете директно към +5V на адаптера.

DHT22 и OLED: свържете към 3.3V (VCC), GND (обща маса) и техните SDA/SCL/DATA пинове както по-горе.

Проверка: преди да натоварвате с вентилатори, първоначално тествайте релето без товар — слушайте щракане при активиране, и измерете с мултицет дали COM свързва NO при активиране.

NodeMCU:

D5 -> IN на Relay (управление)

microUSB -> +5V и GND от адаптера

D2 -> DHT22 DATA

D1/D6 -> OLED SDA/SCL